{
  "name": "astro-gcs-smoke",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "generate:integrations": "node ./scripts/generate-integrations.mjs",
    "dev": "npm run generate:integrations && astro dev",
    "build": "npm run generate:integrations && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "npm run generate:integrations && astro check",
    "deploy:gcs": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/deploy-gcs.ps1"
  },
  "dependencies": {
    "astro": "^6.1.1",
    "gray-matter": "^4.0.3",
    "mermaid": "^11.13.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "typescript": "^5.9.3"
  }
}
